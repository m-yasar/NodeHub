dependencies:
  espressif/mqtt: "^1.0.0"
  # Kurulum sayfasina http://nodehub.local ile ulasmak icin.
  # IDF cekirdeginde degil, kayittan indirilir.
  espressif/mdns: "^1.2"
  espressif/esp_wifi_remote:
    version: ">=0.10,<2.0"
    rules:
      - if: "target in [esp32p4, esp32h2]"
  idf:
    version: ">=5.3"
